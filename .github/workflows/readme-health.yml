name: README Health

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  readme-health:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Check README
        uses: ./
        with:
          readme-path: README.md
          strict: "true"
